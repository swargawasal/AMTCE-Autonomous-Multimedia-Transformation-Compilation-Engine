import crewai
from crewai import Agent, Task, Crew, Process
print(f"CrewAI Version: {crewai.__version__ if hasattr(crewai, '__version__') else 'Installed'}")
print("SUCCESS: Intelligence Swarm Environment is READY.")
